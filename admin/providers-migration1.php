<?php 
exit();
include("sessions.php"); 
/*
$query = "select * from proveedores";
$result = mysqli_query($con, $query);
while($row=mysqli_fetch_array($result)){
	
	$codigo = addslashes($row[codigo]);
	$nombre = addslashes($row[nombre]);
	$direccion = addslashes($row[direccion]);
	$telefono = addslashes($row[telefono]);
	$plazo = addslashes($row[plazo]);
	$ruc = addslashes($row[ruc]);
	$correo = addslashes($row[correo]);
	$contacto = addslashes($row[contacto]);
	$giro = addslashes($row[giro]);
	
	$query2 = "insert into providers (code, name, address, phone, term, ruc, email, cname, course) values ('$codigo', '$nombre', '$direccion', '$telefono', '$plazo', '$ruc', '$correo', '$contacto', '$giro')"; 
	$result2 = mysqli_query($con, $query2);
}
*/
?>